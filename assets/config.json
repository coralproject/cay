{
 "xeniaHost": "${XENIA_URL}",
 "askHost": "${ASK_URL}",
 "trustHost": "${TRUST_URL}",
 "elkhornHost": "${ELKHORN_URL}",
 "environment": "${ENV}",
 "brand": "${PROJECT}",
 "googleAnalyticsId": "${GAID}",
 "requireLogin": false,
 "authClientId": "${AUTH_CLIENT_ID}",
 "authAuthority": "${AUTH_AUTHORITY}",
 "basicAuthorization": "Basic ${AUTH_TOKEN}",
  "features": {
    "trust": ${TRUST},
    "ask": true
  }
}
